const divideUsers = require('../../../app/components/HomeComponent/logicComponent/divideUsers');

test('Test divide users logic component', () => {

    // console.log(divideUsers(14,8));

    const mock = jest.fn();

    expect(mock).toBeCalledWith(expect.anything());

})